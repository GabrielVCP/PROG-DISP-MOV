import { Button } from 'react-native';

const MultiplyButton = ({ onPress }) => {
    return (
        <Button title="Multiply" onPress={onPress} />
    );
};

export default MultiplyButton;
